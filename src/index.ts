import * as readline from 'readline-sync';

function main(){
    console.log("=== Gestor de Alumnos y sus respectivas notas ===")

    const options = ['Crear Usuario', 'Asignar Materia a un alumno', 'Listar materias de un alumno', 'Crear una materia', 'Listar materias', 'Asignar nota de una materia a un alumno', 'Listar alumnos con nota']

    let name: string = readline.questionFloat('Ingrese el nombre del alumno: ')
    let surname: string = readline.questionFloat('Ingrese el nombre del alumno: ')

    
}